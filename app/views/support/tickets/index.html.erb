<% content_for :title, "Your Tickets" %>

<%= link_to "New Ticket", new_support_ticket_path %>

<div id="tickets">
    <% @tickets.each do |ticket| %>
        <div>
            <%= link_to ticket.title, support_ticket_path(ticket) %>
        </div>
    <% end %>
</div>